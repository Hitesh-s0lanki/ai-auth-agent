/**
 * Application-wide constants
 */

/**
 * The number of user messages after which an authentication alert
 * is injected into the conversation for unauthenticated users.
 */
export const AUTH_ALERT_MESSAGE_THRESHOLD = 2;
